
1. look into building and running rails with bundix instead of exec bundle or bin/rails
2. look into tmp issue more closely (I think it happens when trying to run bundix after get all rails dependency gems)